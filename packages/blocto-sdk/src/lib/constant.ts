export enum ERROR_MESSAGE {
  INVALID_TRANSACTION = 'Invalid transaction',
  INVALID_TRANSACTIONS = 'Invalid transactions',
  INVALID_TRANSACTION_VALUE = 'Transaction params "value" should be hex-encoded string',
}
